# modules package — NeuroSignSpeak
# Submodules:
#   emotion_detector    — DeepFace facial emotion recognition
#   asl_recognizer      — MediaPipe ASL finger‑spelling
#   ollama_client       — Ollama LLM integration
#   eeg_stream_simulator — PhysioNet EEG stream replay (Thread B)
#   eeg_processor       — MNE DSP + CSP + SVM classifier (Thread C)
#   fusion              — Weighted decision engine
